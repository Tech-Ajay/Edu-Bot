part of quizzes_web_view.dart;

class _QuizzesMobile extends StatelessWidget {
  final QuizzesController controller;
  const _QuizzesMobile({required this.controller, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
